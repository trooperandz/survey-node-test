<div class="container">
    <div class="row">
        <div class="col-xs-12">
            <h1>Hello!  Please answer the following question:</h1>
            <hr>
            <div class="well">
                <form class="form-horizontal" id="guest-survey-form">
                  <fieldset>
                    <legend>{{question}}</legend>

                    {{!-- Entry feedback --}}
                    <div class="error"></div>
                    <div class="success"></div>

                    <div class="form-group">
                      <div class="col-lg-10">
                      {{!-- Save ChoiceId --}}
                      <input type="hidden" name="ChoiceId" id="ChoiceId" value="{{QuestionId}}">
                      {{#choices}}
                        <div class="radio">
                          <label>
                            <input type="radio" name="choice" id="choice" value={{this.id}}>
                            {{this.choice}}
                          </label>
                        </div>
                      {{/choices}}
                      </div>
                    </div>
                    <div class="form-group">
                      <div class="col-lg-10">
                        <button type="submit" class="btn btn-primary" id="survey-submit-btn">   Submit</button>
                      </div>
                    </div>
                  </fieldset>
                </form>
            </div>
        </div>
    </div>
</div>
